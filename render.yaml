services:
  - type: web
    name: inventory-mcp
    env: docker
    plan: free
    healthCheckPath: /mcp
    envVars:
      - key: MCP_TRANSPORT
        value: sse
      - key: PORT
        value: 8000
